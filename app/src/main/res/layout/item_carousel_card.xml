<?xml version="1.0" encoding="utf-8"?>
<!-- item_carousel_card.xml -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="280dp"
    android:layout_height="180dp"
    android:layout_marginEnd="16dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeWidth="0dp">

    <!-- Fundo colorido / imagem do card -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#111111">

        <!-- Imagem de fundo (opcional, coberta por overlay) -->
        <ImageView
            android:id="@+id/ivCardBg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@null" />

        <!-- Overlay gradiente para legibilidade -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/gradient_card_overlay" />

        <!-- Chip / tag no topo -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipCardTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="Nova vaga"
            android:textSize="11sp"
            android:textColor="#FFFFFF"
            app:chipBackgroundColor="#33FFFFFF"
            app:chipStrokeWidth="0dp"
            app:chipMinHeight="28dp" />

        <!-- Conteúdo na parte inferior -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvCardTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Desenvolvedor Android"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                tools:ignore="HardcodedText"
                xmlns:tools="http://schemas.android.com/tools" />

            <TextView
                android:id="@+id/tvCardSubtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Empresa XYZ · Remoto"
                android:textSize="12sp"
                android:textColor="#CCCCCC"
                tools:ignore="HardcodedText"
                xmlns:tools="http://schemas.android.com/tools" />

        </LinearLayout>

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>